#include <stdio.h>
/**
 *  *main- Serves as the entry point for the program
 *   *
 *    *Return: By default returns 0  if no erros then returns non-zero value.
       */
int main(void)
{
		puts("\"Programming is like building a multilingual puzzle");
			return (0);
}
